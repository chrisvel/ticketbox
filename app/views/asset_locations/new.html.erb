<% provide(:title, 'Add new asset location') %>
<% if user_signed_in? %>
  <div class="row">
    <div class="four wide column">
      <%= render 'layouts/sidebar' %>
    </div>
    <div class="twelve wide column ticket-show">
      <div class="ui segment">
        <%= render 'full_form' %>
      </div>
    </div>
  </div>
<% end %>