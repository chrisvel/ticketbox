<% provide(:title, 'Asset location') %>

<div class="row">
  <div class="four wide column">
    <%= render 'layouts/sidebar' %>
  </div>
  <div class="twelve wide column">
    <div class="ui segment ticket-show">
      
      <div class="ui top left attached teal label">
        Location #<%= @asset_location.id %>
      </div>
     
      <div class="ui items">
        <div class="item">
          <div class="ui tiny image">
            <%= @asset_location.owner.avatar.blank? ? 
              image_tag("no-image.png", alt: "noimage") :
              image_tag(@asset_location.owner.avatar.url(:medium)) 
              %>
          </div>
          <div class="middle aligned content">
            <div class="header"><%= @asset_location.name if @asset_location.name %></div>
            <div class="meta">
              <span class="cinema">
              </span>
            </div>
          </div>
        </div>
      </div>
      <div class="ui divider"></div>
     <div class="ui grid">
       <div class="sixteen wide column">
         <%= render partial: 'asset_location_details' %>
       </div>
     </div>
     <div class="ui divider"></div>
     <div class="">
       <%= link_to '<i class="left chevron icon"></i> Back'.html_safe(), 
         @asset_locations_path, :class => "ui labeled tiny icon button" %>
       <%= link_to '<i class="pencil icon"></i> Edit'.html_safe(),
          edit_asset_location_path(@asset_location), :class => "ui labeled tiny icon teal button"%>
     </div>      
    </div>
  </div>
</div>